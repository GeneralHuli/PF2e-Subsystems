<section class="rp-section">
    <label for="maxRP">Maximum Research Points</label>
    {{#if action.editable}}
        <input data-id="maxRP" name="actions.{{type}}.{{action.id}}.maxRP" type="number" value="{{action.maxRP}}">
    {{else}}
        <div data-id="maxRP">{{action.maxRP}}</div>
    {{/if}}
</section>

<section class="dc-section">
    <label for="dc">DC</label>
    {{#if action.editable}}
        <input data-id="dc" name="actions.{{type}}.{{action.id}}.dc" type="number" value="{{action.dc}}">
    {{else}}
        <div data-id="dc">{{action.dc}}</div>
    {{/if}}
</section>

<section class="description-section">
    <label for="description">Description</label>
    {{#if action.editable}}
        <prose-mirror name="actions.{{type}}.{{action.id}}.description" value="{{action.description}}" toggled="true">
        {{{lookup (lookup (lookup enrichedContent.actions type) action.id) 'description'}}}
        </prose-mirror>
    {{else}}
        {{{lookup (lookup (lookup enrichedContent.actions type) action.id) 'description'}}}
    {{/if}}
</section>